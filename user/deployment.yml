apiVersion: apps/v1
kind: Deployment
metadata:
  name: user
  namespace: roboshop
  labels:
    project: roboshop
    tier: backend
    component: user
spec:
  replicas: 2
  selector:
    matchLabels:
      project: roboshop
      tier: backend
      component: user
  template:
    metadata:
      labels:
        project: roboshop
        tier: backend
        component: user
    spec:
      containers:
      - name: user
        image: mrmotam/user:v1
        env:
        - name: MONGO
          valueFrom:
            configMapKeyRef:
              name: user-cm
              key: MONGO
        - name: MONGO_URL
          valueFrom:
            configMapKeyRef:
              name: user-cm
              key: "MONGO_URL"
        - name: REDIS_URL
          valueFrom:
            configMapKeyRef:
              name: user-cm
              key: "REDIS_URL"
        # resources:
        #   requests:
        #     cpu: "250m"
        #     memory: "256Mi"
        #   limits:
        #     memory: "128Mi"
        #     cpu: "500m"
        ports:
        - containerPort: 8080
